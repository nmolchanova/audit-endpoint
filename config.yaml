variables:
  AUDIT_AGENT_PROFILES_LIST:
      required: false
      type: array
      default:
        - linux-benchmark
        - linux-cis

  AUDIT_AGENT_RULES_LIST:
      required: false
      type: array
      default:
        - echo-hello
        - env-user-password
